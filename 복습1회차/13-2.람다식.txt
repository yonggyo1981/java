1. 함수형 프로그래밍과 람다식
	람다식 -> 익명함수 
	
	자바 -> 함수 자체만 가지고 호출 불가 -> 클래스라는 몸체 -> 메서드 정의 ->
	참조 변수를 통해서 호출 가능
	인터페이스 -> 메서드 -> 단축 문법으로 접근
	(익명 내부 클래스)

2. 람다식 구현하기

3. 람다식 문법 살펴보기(P462)


4. 함수형 인터페이스
무조건 메서드 1개만 정의
@FunctionalInterface 애노테이션 -> 람다식 제한조건에 맞는 인터페이스로 제한


5. 익명 객체를 생성하는 람다식
자바 -> 참조 변수를 통해서 인스턴스의 메서드 접근(함수 자체 호출 X)
익명 내부 클래스(지역 내부 클래스)

6. 함수를 변수 처럼 사용하는 람다식
(매개변수, 반환값)